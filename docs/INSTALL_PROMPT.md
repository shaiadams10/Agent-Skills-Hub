# Install prompt (copy for your AI agent)

Repository: **https://github.com/shaiadams10/AgentSkillsHub**

---

## Short prompt

```
Install Agent Skills Hub from https://github.com/shaiadams10/AgentSkillsHub.

Follow INSTALL.md for my operating system (detect Windows vs macOS yourself).
Ensure every requirement is satisfied — especially Node.js 20+ — installing anything missing.
Then start the app and confirm http://localhost:3000 loads.
```

---

## Detailed prompt

```
I want to install Agent Skills Hub from:
https://github.com/shaiadams10/AgentSkillsHub

Please:
1. Clone the repository.
2. Read INSTALL.md and follow the steps for my OS (detect Windows or macOS; do not ask me which unless detection fails).
3. Verify and satisfy all requirements (Node.js 20+, npm, etc.). Install Node LTS if needed.
4. Run the platform install script if appropriate (scripts/install.ps1 or scripts/install-mac.sh).
5. Start the app using Start Agent Skills Hub.bat (Windows) or Start Agent Skills Hub.command (macOS), or npm run dev.
6. Confirm the UI loads at http://localhost:3000 and tell me how to stop the server.


```
